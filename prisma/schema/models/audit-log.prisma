model AuditLog {
  id          String   @id @default(cuid())
  action      String
  entityId    String
  entityType  String
  actorId     String
  details     String?
  createdAt   DateTime @default(now())
}
